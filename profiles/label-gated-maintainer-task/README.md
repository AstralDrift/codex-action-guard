# label-gated-maintainer-task

Label-gated maintainer task profile for privileged review packets.

Safety properties:

- `pull_request_target` with trusted base checkout.
- maintainer label and author association gate.
- protected environment.
- `allow-users` support.
- artifact output rather than direct writes.
